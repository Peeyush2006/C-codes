#include <stdio.h>

int main()
{
    int marks[50]; // Reserve space to store 50 integers
    marks[0] = 56;
    marks[1] = 76;
    // We can go til the marks [49]
    printf("Marks 0 and Marks 1 is %d %d ", marks[0], marks[1]);

    return 0;
}