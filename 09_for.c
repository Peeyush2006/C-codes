#include <stdio.h>

int main()
{
    int n = 6;

    // Loop runs from i = 0 to i < n (i.e., n-1).
    for (int i = 0; i < n; i++)
    {
        printf("The value of i is %d\n", i);
    }

    return 0;
}
